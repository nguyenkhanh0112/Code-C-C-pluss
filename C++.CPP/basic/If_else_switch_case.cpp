#include<iostream>
#include<math.h>


using namespace std;

//abs(x) : tri tuyet doi 
//pow(a,b): tinh a^b : tra ve so double;
// sqrt(n) : tinh can bac 2 cua n : tra ve so double
// round(a) : lam tron so

int main(){
    float a=2.5632;

    float b=round(a); 
    cout<<b<<endl;
}